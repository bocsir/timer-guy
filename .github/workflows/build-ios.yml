name: build unsigned ipa for side-loading

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v6

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.x"

      - name: Install dependencies
        run: flutter pub get

      - name: Build ios (no codesign)
        run: |
          flutter build ios --release --no-codesign
          cd build/ios/iphoneos
          rm -rf Payload timer_guy.ipa
          mkdir Payload
          cp -r Runner.app Payload/
          zip -r timer_guy.ipa Payload
          mv timer_guy.ipa $GITHUB_WORKSPACE/

      - name: Upload ipa
        uses: actions/upload-artifact@v6
        with:
          name: timer_guy_ipa
          path: timer_guy.ipa
